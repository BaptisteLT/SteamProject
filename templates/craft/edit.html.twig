{% extends 'base.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
    <link href="{{ asset('css/craftedit.css') }}" rel="stylesheet"/>
{% endblock %}

{% block title %}Edit Craft{% endblock %}

{% block body %}
    <div class="container">
        <div class="form_craft_block_edit">
            <h1>Edit Craft</h1>
            <img class="card-img-top" src="{{asset('images/crafts/')}}{{ craft.nomImage }}" alt="Card image cap">
            {{ include('craft/_form.html.twig', {'button_label': 'Update'}) }}

            <a href="{{ path('craft_index') }}">back to list</a>

            {{ include('craft/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
