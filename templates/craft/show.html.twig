{% extends 'base.html.twig' %}

{% block title %}Craft{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link href="{{ asset('css/showcraft.css') }}" rel="stylesheet"/>
{% endblock %}


{% block body %}
    <h1>Craft</h1>

    <table class="table table-striped table-dark">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ craft.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ craft.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ craft.description }}</td>
            </tr>
            <tr>
                <th>Cost</th>
                <td>{{ craft.cost }}</td>
            </tr>
            <tr>
                <th>Verified</th>
                <td>{{ craft.verified ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ craft.dateAjout ? craft.dateAjout|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ craft.nomImage }}</td>
            </tr>
            <tr>
                <th>Updated at</th>
                <td>{{ craft.updatedAt ? craft.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('craft_index') }}">back to list</a>

    <a href="{{ path('craft_edit', {'id': craft.id}) }}">edit</a>

    {{ include('craft/_delete_form.html.twig') }}
{% endblock %}
