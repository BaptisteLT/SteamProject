{% extends 'base.html.twig' %}

{% block title %}New Giveaway{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link href="{{ asset('css/giveawaynew.css') }}" rel="stylesheet"/>
{% endblock %}

{% block body %}
    <div class="container">
        <h1>Create a new giveaway</h1>
        <div class="form-craft-block">
            {{ form_start(form) }}
                {{ form_widget(form) }}
                <button class="btn">{{ button_label|default('Create') }}</button>
            {{ form_end(form) }}

            <a href="{{ path('giveaway_index_admin') }}">back to list</a>
        </div>
    </div>
{% endblock %}
